/**
 * @file app_state.c
 * @brief Translation unit for application state definitions.
 *
 * State-machine behavior can be added in the App layer when needed, while the
 * current firmware keeps Core/Src/main.c and BSP modules independent of app
 * state decisions.
 */
#include "app_state.h"
